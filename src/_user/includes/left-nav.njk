<div class="sidebar-nav">
    <div class="sidebar-nav-header">
        <a href="{{ '/' | url }}" class="site-logo">
            {% if site.logo %}
                <img src="{{ site.logo | url }}" alt="{{ site.title or 'Maja Explosiv' }}" class="logo-image">
            {% else %}
                <span class="logo-text">{{ site.title or "Maja Explosiv" }}</span>
            {% endif %}
        </a>
        {% if site.tagline %}
            <p class="site-subtitle">{{ site.tagline }}</p>
        {% endif %}
    </div>

    <nav class="nav-menu-container" aria-label="Sidebar navigation">
        <ul class="nav-menu">
            <!-- Home Link -->
            <li class="nav-item">
                <a href="{{ '/' | url }}" class="nav-link{% if page.url == '/' %} current{% endif %}">Home</a>
            </li>

            <!-- Projects Section -->
            <li class="nav-section">
                <span class="nav-section-title">Projects</span>
                <ul class="nav-submenu">
                    <li><a href="{{ '/collections/sculptures/' | url }}" class="nav-link">Sculptures</a></li>
                    <li><a href="{{ '/collections/installations/' | url }}" class="nav-link">Installations</a></li>
                    <li><a href="{{ '/collections/performance/' | url }}" class="nav-link">Performance</a></li>
                    <li><a href="{{ '/collections/paintings/' | url }}" class="nav-link">Paintings</a></li>
                </ul>
            </li>

            <!-- About Section -->
            <li class="nav-section">
                <span class="nav-section-title">About</span>
                <ul class="nav-submenu">
                    <li><a href="{{ '/about/bio/' | url }}" class="nav-link">Bio</a></li>
                    <li><a href="{{ '/about/press/' | url }}" class="nav-link">Press</a></li>
                    <li><a href="{{ '/about/links/' | url }}" class="nav-link">Links</a></li>
                </ul>
            </li>

            <!-- Contact Link -->
            <li class="nav-item">
                <a href="{{ '/contact/' | url }}" class="nav-link{% if page.url == '/contact/' %} current{% endif %}">Contact</a>
            </li>
        </ul>
    </nav>

    <div class="sidebar-nav-footer">
        <!-- Footer content for sidebar, if any -->
    </div>
</div>